{
  "name": "@unchartedsoftware/leaflet.zoomhome",
  "version": "0.0.1",
  "description": "Fork with proper package.json so we can add as dependency in npm install",
  "devDependencies": {
    "grunt": "~0.4.5",
    "grunt-contrib-jshint": "~0.4.1",
    "grunt-contrib-uglify": "~0.5.0"
  }
}
